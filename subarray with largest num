# given an integer array 'nums' find the subarray with largest sum and return it's sum
#num=[-2, 1, -3, 4, -1, 2, 1,-5, 4]
#output=6
n=int(input("Enter length of array:"))
arr = list(map(int, input("Enter elements of array: ").split()))

max_sum = arr[0]
current_sum = 0

for i in arr:
    current_sum += i
    if current_sum > max_sum:
        max_sum = current_sum
    if current_sum < 0:
        current_sum = 0

print("Largest subarray sum =", max_sum)
